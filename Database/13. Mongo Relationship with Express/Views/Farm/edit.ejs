<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>

<body>
    <h1>Edit Product</h1>
    <form action="/farm/<%= farm._id %>?_method=PUT" method="post">
        <label for="name">Farm Name</label>
        <input type="text" name="name" placeholder="farm name" value="<%= farm.name  %>">
        <label for="price">Price</label>
        <input type="number" name="price" placeholder="farm price" value="<%= farm.price %>">
        <label for="category">Select Category</label>
        <select name="category">
            <!-- ======== Ternary Operator ======== -->
            <!-- <option value="clothes" <%=product.category==='clothes' ? 'selected' : '' %> >Clothes</option>
            <option value="watch" <%=product.category==='watch' ? 'selected' : '' %> >Smart Watch</option>
            <option value="accessories" <%=product.category==='accessories' ? 'selected' : '' %> >Accessories</option>
            <option value="shoes" <%=product.category==='shoes' ? 'selected' : '' %> >Shoes</option> -->

            <% for(let farm of farms){ %>
                <option value="<%= category %>" <%=farm.category===category ? 'selected' : '' %>>
                    <%= category %>
                </option>
                <% } %>
        </select>
        <button>Submit</button>
    </form>

    <a href="/farm/<%= farm._id %>">Cancel</a>

</body>

</html>
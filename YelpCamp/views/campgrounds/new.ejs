<% layout('layouts/boilerplate') %>

    <form class="pt-20" action="/campground" method="post">
        <div class="mt-2">
            <label for="title">Title</label>
            <input type="text" name="campground[title]" id="title" required>
        </div>
        <div class="mt-2">
            <label for="location">Location</label>
            <input type="text" name="campground[location]" id="location">
        </div>
        <a class="btn btn-success mt-4 mb-4" href="/">Add Campground</a>
    </form>
    <a href="/campground">Back to all campground</a>